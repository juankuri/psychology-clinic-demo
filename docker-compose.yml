services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '4322:4322'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: pnpm dev --host 0.0.0.0

volumes:
  node_modules:
